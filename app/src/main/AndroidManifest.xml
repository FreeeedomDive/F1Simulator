<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.freee.f12018">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo_square"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DriversInformation" />
        <activity android:name=".MenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SelectingRace" />
        <activity android:name=".RaceActivity" />
        <activity android:name=".SelectingRaceForWeekend" />
        <activity android:name=".QualisRound1" />
        <activity android:name=".QualisRound2" />
        <activity android:name=".QualisRound3" />
        <activity android:name=".Statistics" />
        <activity android:name=".ChampionshipLobby" />
    </application>

</manifest>